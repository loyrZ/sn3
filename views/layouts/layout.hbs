<!DOCTYPE html>
<html>

<head>
    <title>{{title}}</title>
    <link rel="icon" href="/public/favicon.ico" sizes="70x70" type="image/ico">
    <link rel='stylesheet' href='/public/css/css.css' />
    <script defer src="/public/js/script.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Quicksand:wght@400&display=swap" rel="stylesheet">
</head>

<body>
<div id="container">

    {{#if showMainHeader}}
        {{> mainHeader}}
    {{/if}}

    {{#if showMainBody}}
        {{>mainBody}}
    {{/if}}

    {{#if showcomingsoon}}
        {{>comingsoonpartial}}
    {{/if}}

    {{#if showMainFooter}}
        {{> mainFooter}}
    {{/if}}

    <!--

<div class="divider"></div>
{>search}  ---------------- need to add another { and } to work in between >search
<div class="divider"></div>
-->
    <!--
    {{#if showThumb}}
        <div id="card-container">
            {{#each posts}}
        {{> card this}}
    {{/each}}
        </div>
    {{/if}}
        {{{body}}}
        {{#if showProfileCard}}
        <div class="divider"></div>
        <div id="card-container">
            {{#each userArray}}
            {{> card_profile this}}
        {{/each}}
        </div>
    {{/if}}
        {{#if showThumbBottom}}
        <div class="divider"></div>
        <div id="card-container">
            {{#each posts}}
            {{> card this}}
        {{/each}}
        </div>
    {{/if}}

        {{#if showComments}}
        <button id="toggle-sort" class="toggle-sort-button">Sort: Newest First</button>
        {{> comment this}}
        {{/if}}
-->

</div>
</body>
</html>
