<body class="body">

    <section class="hero">
        <div class="hero-content">
            <h1>Segar Nyaman</h1>
            <p>Menghadirkan kenyamanan alami untuk tubuh sehat</p>
        </div>
    </section>

    <section class="intro-text" id="about">
        <p>Tempat di mana kenyamanan, kesehatan, dan keindahan berpadu. Kami hadir untuk menghadirkan pengalaman relaksasi terbaik bagi tubuh Anda.</p>
    </section>


    <section class="quote-section">
        <div id="quote-box" class="quote-text">
            <!-- Kutipan akan muncul di sini -->
        </div>
        <div class="quote-dots" id="quote-dots">
            <!-- Dots will be generated by JS -->
        </div>
    </section>

    <div id="image-modal" class="modal">
        <span class="close">&times;</span>
        <img class="modal-content" id="modal-img">
    </div>

    <script>
        const quotes = [
            `"Lebih baik mencegah daripada mengobati." – Peribahasa Indonesia`,
            `"Harta yang paling berharga adalah kesehatan." – Anonim`,
            `"Tubuh yang sehat adalah rumah bagi jiwa yang kuat." – Anonim`,
            `"Bangsa yang kuat adalah bangsa yang rakyatnya sehat." – Soekarno`,
            `"Cinta dapat menyembuhkan, ilmu dapat menyelamatkan, dan teknologi dapat membantu. Tetapi hati yang tulus adalah obat paling mujarab." – BJ Habibie`,
            `"Kalau sakit, Anda tidak akan bisa menikmati kekayaan, keluarga, bahkan ibadah. Maka kesehatan harus jadi prioritas nomor satu." – Tung Desem Waringin`,
            `"Kesehatan bukan segalanya, tapi tanpa kesehatan segalanya tidak berarti." – Arthur Schopenhauer`,
            `"Makanlah untuk hidup, bukan hidup untuk makan." – Socrates`
        ];

        let quoteIndex = 0;
        const quoteBox = document.getElementById('quote-box');
        const dotContainer = document.getElementById('quote-dots');

        function createDots() {
            for (let i = 0; i < quotes.length; i++) {
                const dot = document.createElement('span');
                dot.classList.add('dot');
                dot.dataset.index = i;
                dot.addEventListener('click', () => {
                    quoteIndex = i;
                    showQuote();
                    resetInterval(); // reset auto-change timer
                });
                dotContainer.appendChild(dot);
            }
        }

        function updateDots() {
            const dots = document.querySelectorAll('.quote-dots .dot');
            dots.forEach(dot => dot.classList.remove('active'));
            dots[quoteIndex].classList.add('active');
        }

        function showQuote() {
            quoteBox.style.opacity = 0;
            setTimeout(() => {
                quoteBox.textContent = quotes[quoteIndex];
                quoteBox.style.opacity = 1;
                updateDots();
            }, 300);
        }

        function nextQuote() {
            quoteIndex = (quoteIndex + 1) % quotes.length;
            showQuote();
        }

        let quoteInterval = setInterval(nextQuote, 6000);

        function resetInterval() {
            clearInterval(quoteInterval);
            quoteInterval = setInterval(nextQuote, 6000);
        }

        // Init
        createDots();
        showQuote();

        // Get modal elements
        const modal = document.getElementById("image-modal");
        const modalImg = document.getElementById("modal-img");
        const closeBtn = document.getElementsByClassName("close")[0];

        // Add click event to all service images
        document.querySelectorAll('.service-image img').forEach(img => {
            img.addEventListener('click', function () {
                modal.style.display = "block";
                modalImg.src = this.src;
                modalImg.alt = this.alt;
            });
        });

        // Close modal on click of X
        closeBtn.onclick = function () {
            modal.style.display = "none";
        }

        // Close modal when clicking outside the image
        modal.onclick = function (e) {
            if (e.target === modal) {
                modal.style.display = "none";
            }
        }
    </script>

</body>