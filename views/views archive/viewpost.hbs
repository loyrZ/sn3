<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{currentPost.title}}</title>
</head>
<body class="body">

<div class="main">
    <div class="main content">
        <h4><strong>Title: </strong>{{currentPost.title}}</h4>
        <video controls>
            <source src="/{{currentPost.video}}" type="video/mp4">
            <source src="/{{currentPost.video}}" type="video/webm">
            <source src="/{{currentPost.video}}" type="video/ogg">
            Your browser does not support the video tag.
        </video>
    </div>

    <div class="post-likes">
        <span id="like-button-{{currentPost.id}}" class="fa fa-thumbs-up like-icon" data-postid="{{currentPost.id}}"></span>
        <span id="likes-counter" class="likes-count">{{currentPost.likes}}</span>
    </div>

    <div class="VideoDescription">
        <p><strong>Uploaded By:</strong> {{currentPost.username}}</p>
        <p><strong>Created At:</strong> {{currentPost.created_at}}</p>
        <p class="post-description"><strong>Description:</strong> {{currentPost.description}}</p>
    </div>
</div>

<div class="comment-section">
    <div class="comment-box">
        <textarea id="comment-text" class="comment-box-text" placeholder="Write your comment..."></textarea>
        <button
                id="comment-button" class="fa fa-comment comment-box-button" aria-hidden="true" data-postid="{{currentPost.id}}">
        </button>
    </div>

    <div id="comments" class="comments">
        {{#each comments}}
            <div id="message-{{id}}" class="comment">
                <strong class="comment-author">{{username}}</strong>
                <span class="comment-date">{{formatDate created_at}}</span>
                <div class="comment-text">{{text}}</div>
            </div>
        {{/each}}
    </div>
</div>

<script defer src="/public/js/viewpost.js"></script>
</body>
</html>
